# デフォルトモードを宣言
:: default

# 'window'という新しいモードを宣言（@でキープレスをキャプチャ）
:: window @

# focus window
# |                  | i3-wm       | yabai      |
# | ---------------- | ----------- | ---------- |
# | フォーカスの移動 | $mod + jkl; | alt + hjkl |
alt - / ; window
window < h : yabai -m window --focus west
window < j : yabai -m window --focus south
window < k : yabai -m window --focus north
window < l : yabai -m window --focus east

# move window
# |                    | i3-wm                | yabai               |
# | -------------------| -------------------- | ------------------- |
# | ウィンドウをワープ | $mod + shift + jkl;  | shift + alt + hjkl  |
# | スペースにワープ   | $mod + shift + num   | shift + alt + 12345 |
# | ↑(prev/next)      | --                   | shift + alt + 9/0   |

window < shift - h : yabai -m window --warp west
window < shift - j : yabai -m window --warp south
window < shift - k : yabai -m window --warp north
window < shift - l : yabai -m window --warp east

window < shift - 1 : yabai -m window --space 1
window < shift - 2 : yabai -m window --space 2
window < shift - 3 : yabai -m window --space 3
window < shift - 4 : yabai -m window --space 4
window < shift - 5 : yabai -m window --space 5
window < shift - 9 : yabai -m window --space prev
window < shift - 0 : yabai -m window --space next

# move display
window < ctrl - h : yabai -m display --focus prev
window < ctrl - l : yabai -m display --focus next

window < ctrl - 9 : yabai -m window --display prev
window < ctrl - 0 : yabai -m window --display next

# toggle window native fullscreen
window < shift - f : yabai -m window --toggle native-fullscreen
# toggle window fullscreen zoom
window < f : yabai -m window --toggle zoom-fullscreen
# float / unfloat window and restore position
window < m : yabai -m window --toggle float && yabai -m window --grid 4:4:1:1:2:2

# toggle window split type
window < g : yabai -m window --toggle split

# rotate window
# rotate tree
window < z : yabai -m space --rotate 90
# mirror tree y-axis
window < a : yabai -m space --mirror y-axis
# mirror tree x-axis
window < s : yabai -m space --mirror x-axis

